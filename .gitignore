dist/
node_modules/
.DS_Store
.gstack/
.context/
.env
.env.local
.env.*.local
*.log
scripts/.venv/

# Build-generated photography artifacts. The manifest is regenerated by
# scripts/build-photography-manifest.ts on every `bun run build` and
# `bun run dev` boot; sharp variants under derived/ are regenerated
# on the same step with mtime caching.
media/portfolio/derived/
data/photography-manifest.json

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
