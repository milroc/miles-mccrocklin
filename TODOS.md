# TODOS

- [ ] Photography: curator-author the final intro copy in
      `src/photography/EditorialTopFold.tsx` (`INTRO_LEAD` /
      `INTRO_AFTER` are still the drafted v1 copy). Keep it 1-2
      sentences.
- [ ] Photography: curator picks the 3 hero categories in
      `src/photography/EditorialTopFold.tsx` (`HERO_CATEGORIES` is
      still the recommended starter set from the design review).
- [ ] Media: backfill `alt` text for items in `data/me.json` — see the
      `alt` field contract on `MediaItem` in `src/types.ts`. Render
      paths currently fall back to `caption`.
